# Application
BASE_URL=http://localhost:8080
DEBUG=true
ADMIN_PATH=/admin
HTTPS=false

# Site path (optional — defaults to ../site relative to the CMS engine)
# SITE_PATH=/absolute/path/to/site

# Mail (SMTP)
MAIL_HOST=localhost
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=noreply@localhost
MAIL_FROM_NAME=Station0
MAIL_ENCRYPTION=tls
