<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Forgot password · {{ app.name }}</title>
    <link rel="stylesheet" href="{{ app.adminPath }}/assets/admin.css">
</head>
<body class="admin login-page">
<form class="login-card" method="post" action="{{ app.adminPath }}/forgot-password">
    <input type="hidden" name="{{ csrf.nameKey }}" value="{{ csrf.name }}">
    <input type="hidden" name="{{ csrf.valueKey }}" value="{{ csrf.value }}">
    <h1>{{ app.name }}</h1>
    {% if sent %}
        <p class="success">If that e-mail exists you will receive a password reset link shortly.</p>
    {% else %}
        {% if error %}<p class="error">{{ error }}</p>{% endif %}
        <label>E-mail<input type="email" name="email" autofocus required></label>
        <button type="submit">Send reset link</button>
    {% endif %}
    <a class="link-small" href="{{ app.adminPath }}/login">Back to log in</a>
</form>
</body>
</html>
