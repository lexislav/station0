{% extends 'layout.twig' %}
{% block title %}{{ page.metatitle ?? page.title }} · {{ app.name }}{% endblock %}
{% block body %}
<article class="page">
    <h1>{{ page.title }}</h1>
    <div class="content">{{ content|raw }}</div>
</article>
{% endblock %}
